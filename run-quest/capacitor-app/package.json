{
  "name": "run-quest-app",
  "version": "0.2.2",
  "description": "러닝퀘스트 · Run Quest — 동네 탐험 러닝 앱 Android wrapper (Capacitor)",
  "private": true,
  "scripts": {
    "copy:web": "cp ../prototype/v0.2.2.html www/index.html",
    "icons": "python3 tools/make_icons.py",
    "sync": "npm run copy:web && cap sync android",
    "apk:debug": "npm run sync && cd android && ./gradlew --no-daemon assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
